class Solution(object):
    def reverseWords(self, s):
        """
        :type s: str
        :rtype: str
        """
        forwardList = s.split();
        forwardList.reverse(); 
        return " ".join(forwardList);
